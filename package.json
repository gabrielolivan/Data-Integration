{
  "name": "data-integration-challenge",
  "version": "1.0.0",
  "description": "",
  "main": "Data-Integration.js",
  "scripts": {
    "start": "SET NODE_ENV=start && node -r dotenv/config server.js",
    "linux": "export NODE_ENV=start && node -r dotenv/config server.js",
    "test": "mocha --delay --full-trace -r dotenv/config"
  },
  "author": "Gabriel Olivan",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "fs": "0.0.1-security",
    "mocha": "^8.2.0",
    "sqlite": "^4.0.15",
    "sqlite-async": "^1.1.0",
    "sqlite3": "^5.0.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0"
  }
}
